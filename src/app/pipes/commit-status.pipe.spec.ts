import { CommitStatusPipePipe } from './commit-status-pipe.pipe';

describe('CommitStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CommitStatusPipePipe();
    expect(pipe).toBeTruthy();
  });
});
